---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: dhcpd-debian-stable
    image: debian:stable
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        dhcpd_ignore_docker: no
  lint:
    name: ansible-lint
scenario:
  name: debian-stable
